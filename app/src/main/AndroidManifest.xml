<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:description="@string/module_description"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.BatteryRemapper">

        <!--
            A modern Xposed API module carries no xposed meta-data: the module name comes from
            android:label, the description from android:description, the entry point from
            META-INF/xposed/java_init.list, the scope from META-INF/xposed/scope.list and the
            API requirements from META-INF/xposed/module.prop.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".SettingsProvider"
            android:authorities="com.github.dhangofa.batteryremapper.settings"
            android:enabled="true"
            android:exported="true" />
    </application>

</manifest>
